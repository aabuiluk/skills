# Findings (MCP + local)

- Live hub `https://csctemplate.pythonanywhere.com/courses/python_ai_author`: login 200, 50/50 presentations HTTP 200 with teacher cookie.
- Double chrome «Пояснення · коротко / детально» + «Глибина пояснення»: **25 пар**.
- Same-type depth toggles: **1016** (`data-lh-depth-bar`), ~20 на пару. Кожна теорія 0.2 обгорнута в той самий switch.
- Live pair 18: TOC пункти 05 і 06 — дві карти пари; 07 — блок пояснення; «що робимо сьогодні» ×3 у HTML викладача.
- % джерел: студентські article, див. `notes/report.html`. Сирий перший зріз (з CSS-завищенням лічильника класу) лишився в `raw/`.
