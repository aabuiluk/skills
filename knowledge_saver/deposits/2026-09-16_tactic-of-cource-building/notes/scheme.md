# Distill: prototype-pair17-blocks

Source: `raw/prototype-pair17-blocks.html`.

Four zones: `chrome` | `rail` | `stage` | `notebook`.

Sixteen types (see skill `references/block-types.md`). Gold seed order: `references/pair-layout.md`.

Emit format = prototype `snapshot()` JSON: `{ savedAt, zones: { chrome, rail, stage, notebook } }`.
