# Distill

Leading words for the skill: hook, graft, layer.

Slot states: hole / stub / core / thick.
Layers: required / extra / teacher / take-home (+ material version-from).

Graft search order: hole → stub same fact → sibling after core → overflow extra/version → unmatched hook.
